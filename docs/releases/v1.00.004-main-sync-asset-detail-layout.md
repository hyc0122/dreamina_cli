# v1.00.004 主目录同步与资产详情布局压缩

更新时间：2026-06-20 14:28:54 +08:00

## 更新名称

主目录同步与资产详情布局压缩

## 功能与 Bug

- 修复用户运行主目录时仍看到“未返回图片数据”的错位问题：确认主目录已包含佳速图片生图任务轮询修复。
- 佳速图片生图继续按官方规则提交 model、prompt、params，params 内只提交 size 和 quality。
- 资产管理右侧详情栏从 400px 放宽到 460px，超宽屏为 500px。
- 资产/文件名与别名固定同排显示。
- 详情描述输入框高度下调，减少右侧面板纵向长度。
- 生图模型、画幅、分辨率压缩到同一行显示，标签和参数同排。
- 画风风格与类型前缀模块只保留设置入口，不再展示长提示词预览。

## 修改内容

- 更新 frontend/src/components/jimeng/pages/JimengAssetManagerPage.tsx 的资产详情栏宽度。
- 更新 frontend/src/components/jimeng/assets/AssetDetailPanel.tsx 的右侧详情表单布局。
- 保留主目录 backend/app/llm/client.py 中的佳速接口修复逻辑。

## 验证记录

- 已用本地脚本验证主目录佳速请求体只包含 model、prompt、params，且 params 只包含 size 和 quality。
- 已运行 python -m compileall .\backend\app，后端代码编译通过。
- 已运行 npm run build，前端 TypeScript 与 Vite 构建通过。

## 同步提醒

本次修改已落在主目录 G:\漫剧\LumenX\dreamina_cli。以后每次改完版本，需要确认运行目录是否与修改目录一致，并提醒是否已经同步。