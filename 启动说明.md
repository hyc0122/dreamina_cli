# Dreamina CLI 即梦批量生产独立项目启动说明

当前项目目录：

```text
G:\漫剧\LumenX\dreamina_cli
```

## 启动后端

```powershell
cd G:\漫剧\LumenX\dreamina_cli
.\start_backend.ps1
```

后端地址：

```text
http://127.0.0.1:18177
```

健康检查：

```text
http://127.0.0.1:18177/health
```

## 启动前端

第一次运行先安装依赖：

```powershell
cd G:\漫剧\LumenX\dreamina_cli\frontend
npm install
```

之后启动：

```powershell
cd G:\漫剧\LumenX\dreamina_cli
.\start_frontend.ps1
```

前端地址：

```text
http://127.0.0.1:62100
```

## 不再使用的旧地址

不要再打开：

```text
http://127.0.0.1:62073/#/jimeng
```

这个地址属于旧的 LumenX 主项目开发服务，不是当前独立项目入口。

## 数据位置

独立项目数据保存在：

```text
G:\漫剧\LumenX\dreamina_cli\backend\data
```

包括 SQLite 数据库、上传资产、音色、生成结果和队列记录。
